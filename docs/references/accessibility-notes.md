# Accessibility Notes

MVP accessibility checks are basic and deterministic:

- Require meaningful page title content.
- Prefer visible CTA labels.
- Warn when primary and background colors may have weak contrast.
- Keep exported markup semantic where possible.

Future work should add stronger contrast checks, keyboard navigation testing, and ARIA review.
